<?xml version="1.0" encoding="utf-8"?>

<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/itemCard"
    style="@style/StyleCardOfCategories"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/half_main_dimen"
    android:layout_marginTop="@dimen/main_dimen"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/ivCardImage"
            android:layout_width="match_parent"
            android:layout_height="@dimen/card_view_image_height"
            android:contentDescription="@null"
            android:scaleType="centerCrop"
            tools:src="@drawable/bcg_categories" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:orientation="vertical"
            android:padding="@dimen/half_main_dimen">

            <TextView
                android:id="@+id/tvCardTitle"
                style="@style/StyleTitleOfCard"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="start|top"
                android:layout_marginBottom="@dimen/half_main_dimen"
                tools:text="Заголовок" />

            <TextView
                android:id="@+id/tvCardDescription"
                style="@style/StyleDescriptionOfCard"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                tools:text="Описание" />
        </LinearLayout>

    </LinearLayout>

</androidx.cardview.widget.CardView>